<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="spring.boot.data.mybatis.mapper.UserMapper" >

    <insert id="insert" parameterType="list"
            useGeneratedKeys="true" keyProperty="id">
        INSERT t_user ( name ) VALUES
         <foreach collection ="list" item="user" index= "index" separator =",">
            (#{user.name})
         </foreach >
    </insert>

    <update id="update" parameterType="userPO" >
      <selectKey keyProperty="id" resultType="int" order="BEFORE">
        SELECT * FROM t_user WHERE name=#{name}
      </selectKey>
      UPDATE t_user SET birthday=#{birthday} WHERE name=#{name}
    </update>

    <update id="create">
        DROP TABLE IF EXISTS t_user;
        CREATE TABLE `t_user` (
        `id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        `name` VARCHAR(10) NULL,
        `birthday` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )  ENGINE=INNODB DEFAULT CHARSET=UTF8MB4 COLLATE = UTF8MB4_0900_AI_CI;
    </update>

</mapper>